## Arranque del servidor de la pokedex
Ir a la carpeta monsters-server y abrir una terminal.

Usar el comando: npx json-server db.json --port 3000

En la dirección http://localhost:3000/monsters podrás ver la lista de los monstruos en formato Json.
